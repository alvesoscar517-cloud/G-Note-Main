# ============================================
# Google OAuth - Multiple Clients Support
# ============================================
# Backend hỗ trợ nhiều OAuth clients cho các platforms khác nhau
# Mỗi platform có thể có Client ID và Secret riêng

# Option 1: Single client (backward compatible)
# Dùng cho tất cả platforms
GOOGLE_CLIENT_ID=your_client_id
GOOGLE_CLIENT_SECRET=your_client_secret

# Option 2: Multiple clients (recommended for production)
# Mỗi platform có OAuth client riêng trong Google Cloud Console

# Web App (production domain)
# GOOGLE_CLIENT_ID_WEB=your_web_client_id
# GOOGLE_CLIENT_SECRET_WEB=your_web_client_secret

# Chrome Extension
# GOOGLE_CLIENT_ID_EXTENSION=your_extension_client_id
# GOOGLE_CLIENT_SECRET_EXTENSION=your_extension_client_secret

# Localhost (development)
# GOOGLE_CLIENT_ID_LOCALHOST=your_localhost_client_id
# GOOGLE_CLIENT_SECRET_LOCALHOST=your_localhost_client_secret

# ============================================
# Google API Key (for public Drive file access)
# ============================================
GOOGLE_API_KEY=your_api_key

# ============================================
# CORS - Allowed Origins
# ============================================
# Hỗ trợ nhiều origins, phân cách bằng dấu phẩy
# Hỗ trợ wildcard cho Chrome extensions: chrome-extension://*
# Để trống = cho phép tất cả (chỉ dùng cho development)

# Development (single origin)
ALLOWED_ORIGINS=http://localhost:5173

# Production (multiple origins)
# ALLOWED_ORIGINS=http://localhost:5173,https://gnote.app,chrome-extension://*

# ============================================
# Server Configuration
# ============================================
PORT=8080

# Google Cloud Project (auto-detected on Cloud Run)
# GOOGLE_CLOUD_PROJECT=your-project-id

# ============================================
# Lemon Squeezy (Payment for AI Credits)
# ============================================
# Webhook secret from Settings → Webhooks
LEMONSQUEEZY_WEBHOOK_SECRET=whsec_xxxxxx

# Store ID hoặc slug (từ Lemon Squeezy dashboard)
LEMONSQUEEZY_STORE=yourstore

# Variant IDs (Product → Variants → click variant → lấy ID từ URL)
LEMONSQUEEZY_VARIANT_STARTER=123456
LEMONSQUEEZY_VARIANT_POPULAR=123457
LEMONSQUEEZY_VARIANT_PRO=123458
